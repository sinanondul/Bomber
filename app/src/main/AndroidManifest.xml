<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yusuf.game">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GameManager" />
        <activity android:name=".OutputDialog" />
        <activity
            android:name=".DiceDisplay"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog.MinWidth" />
        <activity android:name=".Selection" />
        <activity android:name=".GameMode" />
        <activity android:name=".CharacterSelect" />
        <activity android:name=".MultiCharSelect" />
        <activity
            android:name=".MainStart"
            android:label="Jungle Pursuit"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Help" />
        <activity android:name=".Credits"></activity>
    </application>

</manifest>